# eCredit Environment Variables for Docker Compose (Local Deployment)
#
# This file is a TEMPLATE for the .env file used by docker-compose.
# Copy this to .env in the root directory for local Docker Compose deployment.
#
# Usage:
#   cp .env.example .env
#   docker-compose up
#
# NOTE: This .env is ONLY for Docker Compose local deployment.
# For local development WITHOUT Docker, each app has its own .env in apps/*/

# ============================================================================
# SECURITY SECRETS (Required - Change in production)
# ============================================================================
JWT_SECRET=secret
SECRET_KEY_BASE=secret

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
POSTGRES_DB=test_db
POSTGRES_USER=test
POSTGRES_PASSWORD=test
POSTGRES_PORT=5432

# ============================================================================
# CACHE/REDIS CONFIGURATION
# ============================================================================
REDIS_PORT=6379

# ============================================================================
# SERVICE PORTS
# ============================================================================
BACKEND_PORT=3000
PROVIDER_PORT=3001
FRONTEND_PORT=5173

# ============================================================================
# API ENDPOINTS (Frontend uses these - from browser perspective)
# ============================================================================
# If you use just start, you can ignore this
VITE_API_URL=http://localhost:3000
API_URL=http://localhost:3000

# ============================================================================
# REALTIME PROVIDER
# ============================================================================
# Options: 'socketio' (for Bun backend) or 'phoenix' (for Elixir backend)
# If you use just start, you can ignore this
VITE_REALTIME_PROVIDER=socketio
