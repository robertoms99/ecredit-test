# ============================================================================
# Frontend Environment Configuration - Local Development Only
# ============================================================================
# Copy this file to .env in the apps/frontend directory for local development
# This .env is ONLY for local development without Docker
# For Docker Compose deployment, see /.env.example in the root directory
# ============================================================================

# ============================================================================
# BACKEND SELECTION - Choose which backend to connect to
# ============================================================================
# The frontend can connect to either the Bun backend  or the
# Elixir/Phoenix backend,frontend connects to only one at a time.

# Backend API URL
VITE_API_URL=http://localhost:3000

# Real-time Provider (WebSocket/Real-time Communication)
# - Set to "socket.io" for Bun backend
# - Set to "phoenix" for Elixir/Phoenix backend
#
# Connection Details:
#   - Socket.IO (Bun): Connects to http://localhost:3000 with socket.io client
#   - Phoenix (Elixir): Connects to ws://localhost:3000/ws with WebSocket
VITE_REALTIME_PROVIDER=socket.io

# ============================================================================
# ENVIRONMENT
# ============================================================================
VITE_NODE_ENV=development
