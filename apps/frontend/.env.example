# ============================================================================
# Frontend Environment Configuration - Local Development Only
# ============================================================================
# Copy this file to .env in the apps/frontend directory for local development
# This .env is ONLY for local development without Docker
# For Docker Compose deployment, see /.env.example in the root directory
# ============================================================================

# ============================================================================
# BACKEND SELECTION - Choose which backend to connect to
# ============================================================================
# The frontend can connect to either the Bun backend (Port 3000) or the
# Elixir/Phoenix backend (Port 4000). Both can run simultaneously, but the
# frontend connects to only one at a time.

# Backend API URL
# - Set to http://localhost:3000 for Bun backend (default) ← Most common
# - Set to http://localhost:4000 for Elixir/Phoenix backend
VITE_API_URL=http://localhost:3000

# Real-time Provider (WebSocket/Real-time Communication)
# - Set to "socket.io" for Bun backend
# - Set to "phoenix" for Elixir/Phoenix backend
#
# IMPORTANT: This MUST match your VITE_API_URL backend selection:
#   - If VITE_API_URL=http://localhost:3000 → Use VITE_REALTIME_PROVIDER=socket.io
#   - If VITE_API_URL=http://localhost:4000 → Use VITE_REALTIME_PROVIDER=phoenix
#
# Connection Details:
#   - Socket.IO (Bun): Connects to http://localhost:3000 with socket.io client
#   - Phoenix (Elixir): Connects to ws://localhost:4000/ws with WebSocket
VITE_REALTIME_PROVIDER=socket.io

# ============================================================================
# ENVIRONMENT
# ============================================================================
VITE_NODE_ENV=development
